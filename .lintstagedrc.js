module.exports = {
  'src/**/*.ts?(x)': 'eslint --fix',
  'src/**/*.scss': 'stylelint --fix',
  // Not required for the current project
  // 'src/**/*.js?(x)': 'eslint --fix',
  // 'src/**/*.css': 'stylelint --fix'
};